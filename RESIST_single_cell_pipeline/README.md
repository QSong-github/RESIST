# The data include the xxx.rds or xxx.RDS file to be load, here sample data GSE104987 is human sample as an example
# ref_data include the database files to be load
# scripts inlcude all the steps of R scripts for human samples
# results inlcuding all the results, text files, pdf and png files, run rename.sh to remove the GSExxxxx prefix
# sbatch run.sh to run all the scripts, may need install several R packages if not in your R path, main R using module load R/4.5


